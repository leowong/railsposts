<%- if @posts[0] && params[:source] -%>
  <%- title "Recent posts from " + link_to(params[:source], @posts[0].news_feed.homepage, :target => "_blank")  -%>
<%- end -%>
<%- javascript :defaults -%>

<div id="posts" class="clearfix">
  <%= render :partial => @posts %>
</div>

<div id="pagination" class="pagination clearfix">
  <% if @posts.current_page < @posts.total_pages %>
    <%= render :partial => "more", :locals => { :page => @posts.current_page + 1 } %>
  <% else %>
    <hr class="space" />
    <div>
      End of messages
    </div>
  <% end %>
</div>
